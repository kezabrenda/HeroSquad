{{#partial "content"}}

    <h1>Hero list!</h1>

    {{#if posts}} <!--if posts exist, then -->
    {{#each posts }} <!--loop-->
    <p>Current heroes:</p>
    <p>{{ content }} <a href="/hero/{{id}}">read more...</a></p> <!--show-->
    {{/each}}
    {{else}} <!--no posts!-->
    {{/if}}

    <form action="/hero/new" method="POST">
        <!--<a href="/hero-form"><button class="">Create Hero</button></a>
        <label for="content">Post Content</label>
        <input id="content" name="content" type="text">-->

        <button type="submit" class="btn btn-default">Add your hero</button>
    </form>
{{/partial}}

{{> layout.hbs}}